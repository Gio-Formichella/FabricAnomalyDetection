\documentclass{beamer}
\usepackage{graphicx} % Required for inserting images
\usetheme{Madrid}

\usepackage{biblatex}
\addbibresource{refs.bib}

\title{Fabric Anomaly Detection}
\author{Jacopo Dardini \\ 
Gio Formichella}
\date{}

\setbeamertemplate{navigation symbols}{}  % Removes navigation symbols

\begin{document}

\frame{\titlepage}

\begin{frame}{Prato}
    \begin{columns}
        \begin{column}{0.5\textwidth}
            \begin{itemize}
                \item Fabric anomaly detection
                \item \textbf{Goal}: automate detection of mistakes in the production of cloth and textile material.
            \end{itemize}
            \begin{figure}
                \centering
                \includegraphics[height=0.43\textheight,keepaspectratio]{imgs/giornale.png}
            \end{figure}
        \end{column}
        \begin{column}{0.5\textwidth}
            \begin{figure}
                \centering
                \includegraphics[width=1\linewidth]{imgs/storia_tessile_prato.png}
            \end{figure}
            \begin{figure}
                \centering
                \includegraphics[width=1\linewidth]{imgs/macchinari.jpg}
            \end{figure}
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{Why unsupervised learning?}
    \begin{itemize}
        \item The challenge with supervised learning in the textile industry is the \textbf{scarcity} of labeled anomalous data:
        \begin{itemize}
            \item Actively producing anomalous fabric is counterintuitive.
            \item Fabric design changes seasonally; at each shift, new anomalous data needs to be produced.
        \end{itemize}

        \vspace{0.3cm}
        \item Instead, unsupervised techniques like autoencoders exploit the large quantity of flawless fabric images and detect anomalies through reconstruction error. We tested:
        \begin{itemize}
            \item \textbf{Convolutional autoencoders (CAE)}
            \item \textbf{Variational autoencoders (VAE)}
        \end{itemize}
    \end{itemize}
\end{frame}

\begin{frame}{Data}
\only<1>{
    \begin{itemize}
        \item We choose the fabric section of the \textbf{MVTec AD 2}\cite{MVTecAD2}.
        \item Dataset characteristics:
            \begin{itemize}
                \item Multiple different lighting conditions. Developed models will be robust to real-world distribution shifts due to illumination changes.
                \item High variability textures.
                \item Anomalies distributed across the entire height and width of the images and also directly appear on the image borders.
            \end{itemize}
        \item Defects:
        \begin{itemize}
            \item Cuts and holes.
            \item Color inconsistencies.
            \item Loose thread and extra fabric pieces.
        \end{itemize}
        \item \textbf{Main challenges}:
        \begin{itemize}
            \item Tiny defects with low contrast.
            \item High variability of the normal data due to varying pattern of the print.
        \end{itemize}
    \end{itemize}
}
\only<2-3>{
    \begin{itemize}
        \item Can you spot the anomalies?
    \end{itemize}
    \begin{columns}<2-3>
        \begin{column}{0.5\textwidth}
            \begin{figure}
                \centering
                \includegraphics[width=1\linewidth]{imgs/005_overexposed.png}
            \end{figure}
        \end{column}
        \begin{column}{0.5\textwidth}<3>
            \begin{figure}
                \centering
                \includegraphics[width=1\linewidth]{imgs/005_overexposed_mask.png}
            \end{figure}
        \end{column}
    \end{columns}
}
\end{frame}

\begin{frame}{High resolution challenge}
    \begin{itemize}
        \item Images have a high resolution ($2448 \times 2048$), causing VRAM usage to exceed our GPU limits. Aggressive striding would result in dead filters~\cite{ZFNet}, so we tried two different approaches: \begin{enumerate}
            \only<1, 3>{
            \item \textbf{Resizing} to 256 x 256: \begin{itemize}
                \item We verified that anomalies are not washed away by downsampling.
                \item \textit{Pros}: Allows for inference in a single forward pass.
                \item \textit{Cons}: Anomalies are made even smaller, making them more challenging.
            \end{itemize}
            }
            \only<2>{
            \setcounter{enumi}{1}
            \item Applying the autoencoder as a $256 \times 256$ \textbf{sliding window}:\begin{itemize}
                \item \textit{Pros}: Does not require downsampling.
                \item \textit{Cons}: Requires multiple forward passes for each image.
                \item With a window stride of 256 (each pixel processed once) we got a checkerboard effect.
                \begin{figure}
                    \centering
                    \includegraphics[width=0.3\textwidth]{imgs/checkerboard.png}
                \end{figure}
                \item Pixels at the borders of the window are not reconstructed correctly, increasing their anomaly scores. 
            \end{itemize}
            }
            \only<3>{
            \item Applying the autoencoder as a 256 x 256 \textbf{sliding window}:\begin{itemize}
                \item \textit{Pros}: Does not require downsampling.
                \item \textit{Cons}: Requires multiple forward passes for each image.
                \item We reduce the window stride to 128 and average the overlapping pixel predictions.
                \item We also make the implicit assumption that anomaly detection requires only local information (i.e., information within the window).
            \end{itemize}
            }
        \end{enumerate}
    \end{itemize}
\end{frame}

\begin{frame}{Losses}
    \begin{itemize}\small
        \item \textbf{Mean Squared Error (MSE)} 
        \begin{equation*}
            \mathcal{L}_{MSE}(x, \hat{x}) = \frac{1}{N}\sum_{i=1}^{N} (x_i - \hat{x}_i)^2
        \end{equation*} \begin{itemize}
            \item Teaches the autoencoder to reconstruct absolute pixel values.
            \item Tends to average out high-frequency details to minimize the overall loss, resulting in blurry reconstructions.
        \end{itemize}
        \item \textbf{Structural Similarity Index Measure (SSIM)} \begin{itemize}
            \item Evaluates local luminance, contrast, and structure variations.
            \begin{gather*}
                SSIM(x, \hat{x}) = \frac{(2\mu_x \mu_{\hat{x}} + c_1)(2 \sigma_{x \hat{x}} + c_2)}{(\mu_x^2 + \mu_{\hat{x}}^2 + c_1)(\sigma_x^2 + \sigma_{\hat{x}}^2 + c_2)}\\
                \mathcal{L}_{SSIM} = 1 - SSIM
            \end{gather*}
            \item Added as a complementary term to the pixel-wise $\mathcal{L}_{\text{MSE}}$ to better preserve fabric textures.
        \end{itemize}
    \end{itemize}
\end{frame}

\begin{frame}{Evaluation}
    \begin{itemize}
        \item Each image receives one aggregated anomaly score.
        \begin{itemize}
            \item CAE: mean of the top $1\%$ pixel reconstruction errors.
            \item VAE: mean of the top $0.2\%$ highest-error patches after patch-position normalization.
        \end{itemize}
        \vspace{0.15cm}
        \item The validation set contains no anomalous data for threshold calibration. The models are therefore evaluated using:
        \item \textbf{Area Under the Receiver Operating Characteristic (AUROC)} \begin{itemize}
            \item The area under the ROC curve.
            \item It represents the probability that a randomly chosen anomalous image will have a higher anomaly score than a randomly chosen normal image.
        \end{itemize}
        \item \textbf{Precision-Recall Area Under the Curve (PR AUC)}\begin{itemize}
            \item Also known as Average Precision (AP)
        \end{itemize}
    \end{itemize}
\end{frame}

\begin{frame}{CAE}
    \begin{itemize}
        \item Architecture:
            \begin{itemize}
                \item Stem: $3 \times 3$ convolution mapping the input to 16 channels.
                \item Encoder: 5 convolutional blocks; each doubles the channel depth and halves the spatial resolution.
                \item \textbf{Bottleneck:} Latent space representation of $8 \times 8 \times 512$.
                \item Decoder: 5 convolutional blocks; each halves the channel depth and doubles the spatial resolution through transposed convolutions.
                \item Head: $1 \times 1$ convolution mapping channels back to RGB.
                \item \textbf{No Skip Connections:} Omitted intentionally as they learning the identity function, and reconstruct anomalies.
            \end{itemize}
        \item Optimizer: AdamW~\cite{AdamW} with a learning rate of $10^{-3}$.
        \item Data augmentation: random horizontal and vertical flips.
        \item Trained for 50 epochs
    \end{itemize}
\end{frame}

\begin{frame}{CAE - quantitative results}
    \begin{table}
        \centering
        \footnotesize
        \begin{tabular}{|c|c|c|c|}
        \hline
            Loss & Inference & AUROC & PR AUC\\
        \hline
            MSE & Resize & 0.61 & 0.61\\
        \hline
            SSIM+MSE & Resize & 0.63 & 0.61\\
        \hline
            MSE & Sliding window & 0.67 & 0.69\\
        \hline
            SSIM+MSE & Sliding window & \textbf{0.68} & \textbf{0.71}\\
        \hline
        \end{tabular}
        \caption{Test set scores}
    \end{table}
    \begin{itemize}
        \item Sliding windows preserve more local detail than resizing, but still produce only moderate ranking quality.
        \item SSIM brings slight improvements.
    \end{itemize}
\end{frame}

\begin{frame}{CAE - qualitative diagnosis}
    \begin{figure}
        \centering
        \includegraphics[width=0.92\textwidth]{imgs/cae_predictions.png}
    \end{figure}
    \begin{itemize}\footnotesize
        \item Performance analysis:
        \begin{itemize}
            \item Suboptimal results due to the difficulty of distinguishing between anomalies and misreconstructions.
            \item \textbf{Low-contrast anomalies} get low anomaly scores.
            \item \textbf{High variability in normal data} causes reconstruction errors in non-anomalous regions.
        \end{itemize}
    \end{itemize}
\end{frame}

\begin{frame}{CAE - ablation}
    \begin{figure}
        \centering
        \includegraphics[width=1\textwidth]{imgs/cae_bottleneck_auroc.png}
    \end{figure}
\end{frame}

\begin{frame}{CAE - sanity check}
    \begin{itemize}
        \item The same reconstruction pipeline performs better on a simpler object categories.
        \item AUROC on MVTec AD~\cite{MVTecAD1} bottles is almost 0.85.
    \end{itemize}
    \begin{figure}
        \centering
        \includegraphics[width=0.9\textwidth]{imgs/bottle.png}
    \end{figure}
\end{frame}

\begin{frame}{VAE - architecture}
    \begin{itemize}
        \item Input: one local feature vector at a time.
        \item Encoder: fully connected layers with LayerNorm and GELU activations.
        \begin{itemize}
            \item maps the input to $\mu, \log \sigma^2 \in \mathbb{R}^{32}$
        \end{itemize}
        \item Latent sample: $z = \mu + \sigma \epsilon$, with $\epsilon \sim \mathcal{N}(0, I)$.
        \item Decoder: symmetric MLP reconstructing the original feature vector.
    \end{itemize}
\end{frame}

\begin{frame}{VAE - input features}
    \begin{columns}
        \begin{column}{0.52\textwidth}
            \begin{itemize}
                \item Images are center-cropped, resized to $256 \times 256$, and split into $8 \times 8$ patches.
                \item Each image produces a $32 \times 32$ patch grid.
                \item Each patch feature vector contains:
                \begin{itemize}
                    \item normalized gray-level intensities;
                    \item horizontal and vertical gradients;
                    \item low-frequency FFT magnitudes.
                \end{itemize}
            \end{itemize}
        \end{column}
        \begin{column}{0.48\textwidth}
            \begin{table}
                \centering
                \footnotesize
                \begin{tabular}{|c|c|}
                \hline
                    Component & Dimension\\
                \hline
                    Gray & 64\\
                \hline
                    dx + dy & 112\\
                \hline
                    FFT & 16\\
                \hline
                    Total & 192\\
                \hline
                \end{tabular}
            \end{table}
        \end{column}
    \end{columns}
\end{frame}

\begin{frame}{VAE - training and scoring}
    \begin{itemize}
        \item Training objective:
        \begin{equation*}
            \mathcal{L}_{VAE} = \mathcal{L}_{MSE}(f, \hat{f}) + \beta D_{KL}\left(q(z|f)\|p(z)\right), \quad \beta = 10^{-3}
        \end{equation*}
        \item Denoising noise $\sigma=0.05$ is added during training.
        \item Anomaly score: average of the top $0.2\%$ patch errors, normalized by the expected error at each patch position.
    \end{itemize}
\end{frame}

\begin{frame}{VAE - results}
    \begin{table}
        \centering
        \footnotesize
        \begin{tabular}{|c|c|c|}
        \hline
            Model & AUROC & PR AUC\\
        \hline
            Feature VAE & \textbf{0.83} & \textbf{0.86}\\
        \hline
            High-res Feature VAE & 0.79 & 0.76\\
        \hline
            Best CAE & 0.68 & 0.71\\
        \hline
        \end{tabular}
        \caption{Held-out image-level scores}
    \end{table}
    \begin{itemize}\footnotesize
        \item Best setting: latent dimension 32, $\beta=10^{-3}$, denoising noise $\sigma=0.05$.
        \item High-res variant: $1024 \times 1024$ inputs with $32 \times 32$ patch features.
        \item Patch-feature modeling improves image-level ranking over the CAE baseline.
    \end{itemize}
\end{frame}

\begin{frame}{VAE - diagnostics}
    \begin{figure}
        \centering
        \includegraphics[height=0.60\textheight]{imgs/vae_reconstruction_comparison.png}
    \end{figure}
    \begin{itemize}\footnotesize
        \item The VAE improves image-level ranking over the CAE, but localization remains noisy.
        \item Heatmaps use a single shared color scale across all rows.
        \item Rare normal texture variations can receive high scores, creating false-positive patches.
    \end{itemize}
\end{frame}

\begin{frame}{VAE - ablation study}
    \begin{table}
        \centering
        \footnotesize
        \begin{tabular}{|c|c|c|}
        \hline
            Features & Best AUROC & Best PR AUC\\
        \hline
            Gray + dx/dy + FFT & \textbf{0.83} & \textbf{0.86}\\
        \hline
            dx/dy + FFT & 0.78 & 0.78\\
        \hline
            Gray + dx/dy & 0.76 & 0.76\\
        \hline
            Gray only & 0.72 & 0.71\\
        \hline
        \end{tabular}
        \caption{Feature ablation on held-out image-level scores}
    \end{table}
    \begin{itemize}
        \item Full patch features give the strongest ranking after retraining.
        \item FFT and gradient features remain the strongest non-gray subset.
        \item Gray intensities alone are weaker, but still informative.
    \end{itemize}
\end{frame}

\begin{frame}{Thresholding and deployment gap}
    \begin{itemize}
        \item AUROC and PR AUC measure ranking, not a deployed decision rule.
        \item In production, a threshold is still required:
        \begin{itemize}
            \item too low: many normal texture changes trigger alarms;
            \item too high: small low-contrast defects are missed.
        \end{itemize}
        \item Because validation contains only normal images, threshold calibration would require either:
        \begin{itemize}
            \item a small labeled anomalous validation set;
            \item a target false-positive rate on normal validation images;
            \item human review of uncertain high-score cases during deployment.
        \end{itemize}
    \end{itemize}
\end{frame}

\begin{frame}{Conclusion}
    \begin{itemize}
        \item Reconstruction-based anomaly detection is viable for fabric inspection, but the dataset is challenging:
        \begin{itemize}
            \item defects are often tiny and low-contrast;
            \item normal fabrics have large texture and illumination variability;
            \item no anomalous validation set is available for threshold calibration.
        \end{itemize}
        \item Textile anomaly detection remains an important and open problem for the Prato textile industry.
    \end{itemize}
\end{frame}

\begin{frame}{References}
    \printbibliography
\end{frame}

\end{document}
